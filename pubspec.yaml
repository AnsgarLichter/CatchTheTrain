name: catchthetrain
description: .


publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  meta: '>=1.1.0 <2.0.0'
  shared_preferences:

  #Icons
  cupertino_icons: ^0.1.3
  flutter_icons: ^1.1.0



  english_words: ^3.1.0


  http: ^0.13.5

  #Database
  sqflite: ^1.0.0
  path_provider: ^0.4.1

  # Libs for redux pattern
  flutter_redux: 0.6.0
  redux: 4.0.0

  #Snackbar
  flushbar: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/train.jpg
    - i18n/en.json
    - i18n/de.json
